import javax.swing.*;
import java.awt.*;

/**
 * The Run class is a simple launcher class responsible for starting the application.
 * It creates a new instance of MainScreen to initialize the main user interface.
 */
public class Run {

    /**
     * Starts the application by creating a new MainScreen instance.
     */
    public void run() {
        new MainScreen();
    }
}
